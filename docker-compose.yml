version: "3.8"

services:
  gratify:
    build: .
    container_name: gratify-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./.env:/app/.env:ro
      - ./audit_log.json:/app/audit_log.json
